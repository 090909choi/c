#include<stdio.h>

int main(){
	char str[1000];
	scanf("%s",str);
	for(int i = 0;i<str;i++){
		if('a' <= str && str <= z){
			str[i] = str[i] - 'a' + 'A'
		}
	}
	printf("%d",str);
	return 0;
}
