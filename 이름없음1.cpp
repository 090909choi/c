#include<stdio.h>
#include<string.h>

int main()
{	
	int count,length;
	char a[];
	gets(a);
	length = strlen(word);
	for(int i = 0;i<=length;i++){
	count+=1;
	}
	printf("%d",count+1);
	return 0;
}

